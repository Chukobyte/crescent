#pragma once

// Parallax System
// A system that directly modifies the parallax node's transform to achieve a parallax effect for all attached children

struct EntitySystem* parallax_ec_system_create();
